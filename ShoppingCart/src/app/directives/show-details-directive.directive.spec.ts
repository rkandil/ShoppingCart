import { ShowDetailsDirectiveDirective } from './show-details-directive.directive';

describe('ShowDetailsDirectiveDirective', () => {
  it('should create an instance', () => {
    const directive = new ShowDetailsDirectiveDirective();
    expect(directive).toBeTruthy();
  });
});
